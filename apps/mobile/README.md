# Paashupatastra Mobile (Flutter)

Flutter app will live here.

## Planned structure
- Resident home + Parking module (Phase 1)
- Partner mode later (owners / drivers / technicians)

## Setup (when ready)
```bash
flutter create . --org in.paashupatastra --project-name paashupatastra
```

Do not add this folder to npm workspaces — Flutter uses its own toolchain.
